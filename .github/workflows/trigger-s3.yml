name: Trigger S3 Upload

on:
  push:
    branches:
      - main
  workflow_dispatch:    

jobs:
  call-reusable:
    uses: UnityGames-UPS/unity-build-pipeline/.github/workflows/main.yml@main
    with:
      s3_bucket: unity.build-prod
      s3_folder: SL-BS
      cloudfront_distribution_id: ERDVMYSDM278F
      invalidate_paths: "/SL-BS/*"
